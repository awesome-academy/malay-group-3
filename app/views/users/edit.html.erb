<% provide :title, "edit user" %>
<% provide :button_text, t("users.update.update") %>
<h1>Update your profile</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
  <%= render "form"%>
    <div class="gravatar_edit">
      <%= gravatar_for @user %>
      <%= link_to t("users.update.gravatar"), Settings.links.gravatar_email%>
    </div>
  </div>
</div>
