<% provide :title, "All courses" %>

<h1>All courses </h1>

<%= paginate @course %> 

<ul class="courses">
  <%= render @course %>
</ul>

<%= paginate @course %>

<% if current_user.admin? %>
  <%= link_to "Create New Course", new_course_url %>
<% end %>
